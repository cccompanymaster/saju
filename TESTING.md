# 테스트 방법

키가 하나도 없어도 **전 과정이 mock(데모)으로 동작**하므로 그대로 테스트할 수 있습니다.

## 0) 준비 (1번만)

```bash
cd server
npm install
npx playwright install chromium   # PDF 생성 + E2E 테스트용 (선택이지만 권장)
```

## 1) 직접 띄워서 손으로 테스트 (가장 직관적)

```bash
cd server
npm start
```

브라우저에서 **http://localhost:3000/m/** 접속 후:

1. 우상단 **ENG/KOR** 토글로 언어 전환 확인
2. 이름·생년월일 입력 → **무료 사주 맛보기** → 명식(네 기둥)·오행·신강/용신·맛보기 확인
3. 질문·이메일 입력 → **3,900원으로 심층 사주 열기** (키 없으면 결제창 없이 mock 승인) → 심층 10항목 + **PDF 내려받기** + 발송 안내 + **재열람 링크** 확인
4. 재열람 링크(`/m/?r=...`) 새 탭으로 열어 결과 복원 확인

> 데모 모드에선 결제·AI·발송이 mock 입니다(실제 청구·발송 없음).

## 2) 자동 테스트

```bash
cd server
npm test        # 단위/스모크 (사주 계산·리딩·결제검증·발송 mock)
npm run test:e2e   # 브라우저 E2E (서버 자동 기동 → 퍼널 전 과정 → 종료)
```

`test:e2e` 통과 예시:
```
✓ 기본 언어가 영어
✓ 무료 사주 결과 표시
✓ 심층 리딩 항목 8개 이상
✓ 재열람으로 결과 복원
✓ 전체 통과 (10)
```
(chromium 미설치 시 E2E는 자동 skip → `npx playwright install chromium` 후 재실행)

## 3) 실연동까지 테스트 (실제 키)

`server/.env` 에 키를 채우면 해당 기능이 mock → 실연동으로 바뀝니다.

| 켜는 것 | 필요한 키 | 테스트 포인트 |
|---|---|---|
| AI 해석 | `ANTHROPIC_API_KEY` | 맛보기/심층이 mock 문구가 아닌 실제 생성문으로 |
| 실결제 | `TOSS_CLIENT_KEY` `TOSS_SECRET_KEY` | 결제창 표시 → **테스트 카드**로 승인 (Toss 대시보드 테스트키 사용) |
| 이메일 | `SMTP_*` | 받은편지함에 PDF 첨부 메일 도착 |
| 재열람 링크 | `APP_BASE_URL` | 메일/화면 링크가 실제 도메인으로 |
| 카카오 로그인·채널 | `KAKAO_JS_KEY` `KAKAO_CHANNEL_ID` | 로그인/채널추가 버튼 노출·동작 |
| 알림톡 | `SOLAPI_*` `KAKAO_PFID` `KAKAO_TEMPLATE_ID` | 카카오톡으로 알림톡 수신 (템플릿 사전 승인 필요) |

> 결제는 반드시 **Toss 테스트 키**로 먼저 검증하세요(실 결제 방지). 알림톡은 비즈채널·템플릿 승인이 끝나야 발송됩니다.
