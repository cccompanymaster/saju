# 운툰 (UNTOON) — 운세를 웹툰처럼

MZ세대를 위한 모바일 우선 AI 사주/운세 콘텐츠 플랫폼 데모.
홈은 운세 콘텐츠 마켓처럼, 상세/진단/결과는 웹툰형 상담사 캐릭터 **월아**와 함께하는
한 편의 짧은 상담처럼 느껴지도록 구성했습니다.

> 가상의 신규 브랜드·캐릭터·문구로 제작된 데모이며, 결과는 오락·참고용입니다.

## 기술 스택
- **React 18 + TypeScript + Vite**
- **Tailwind CSS v4** (`@tailwindcss/vite`)
- **Framer Motion** — 화면 전환, 캐러셀, 말풍선 등장 애니메이션
- 실제 이미지 없이 **CSS 그라데이션 포스터 + SVG 캐릭터**로 비주얼 구성

## 실행
```bash
npm install
npm run dev      # 개발 서버
npm run build    # 타입체크 + 프로덕션 빌드
npm run preview  # 빌드 미리보기
```
데스크톱에서는 중앙에 390~430px 모바일 앱 프레임으로 정렬됩니다.

## 화면 구성
- **홈** — 가로 카테고리 탭, 히어로 캐러셀(세로 웹툰 포스터), 카테고리 섹션,
  3분 운세 랭킹, 리얼 후기 피드
- **상세** — 상담사 캐릭터 말풍선 인트로(웹툰 컷) → 3단계 입력 → 무료 미리보기 리포트
  - 1단계: 이름/생년월일/양력·음력/태어난 시간/성별
  - 2단계: 관심사 + 연애·직업 상태
  - 3단계: 자유 질문(선택, 200자)
- **결과(무료 미리보기)** — 사주 명식표 · 오행 그래프 · 대운표 · 시기별 재물/연애 흐름 그래프
  · 한 줄 성향 해석 · 잠금(블러) 프리미엄 카드 · 쿠폰/카운트다운 결제 후크
- **질문하기 / 검색 / 보관함** + 하단 고정 글래스 내비
- 첫 방문 쿠폰 팝업("24시간 동안 보지 않기"), 정통 상품은 네이비·달빛·한지 테마

## 회원가입 · 결제
- **회원가입/로그인** — 이메일·비밀번호 가입 + **카카오 간편 로그인**, 약관·개인정보 동의
- **로그인 게이트** — 결제 시 비로그인이면 로그인 모달을 띄우고, 성공하면 결제를 이어서 진행
- **결제** — 카카오페이/카드, 첫 방문 쿠폰(-3,000원) 자동 적용, PG 승인 흉내 → 주문 생성
- **잠금 해제** — 결제 완료 시 프리미엄 심층 해석(블러 카드)이 전체 공개되고 보관함에 보관
- **보관함** — 계정 정보 + 결제한 리포트 목록, **재열람 2주(14일)** 만료 관리
- 모든 상태는 `src/store.tsx`(Context)에서 관리하고 localStorage에 영속 — 데모용

## 데이터 / API 교체
모든 콘텐츠·상태는 한 곳에 모아 두어 교체가 쉽습니다.
- `src/data.ts` — `products`, `sections`, `ranking`, `reviews` → 상품/큐레이션 API로 교체
- `buildReport(name, topic)` → 실제 리포트 생성 API로 교체
- `src/store.tsx` — `signup/login/loginKakao`(인증 API/OAuth), `purchase`(결제 PG),
  주문·쿠폰 영속(`read`/`write` helper만 교체하면 서버 API로 전환)

## 구조
```
src/
  data.ts                 # 샘플 데이터 + 타입 + 리포트 팩토리
  ui.tsx                  # Poster / RatingStars / 아이콘 / 모션 variants
  App.tsx                 # 앱 셸 · 탭/디테일 상태 · 하단 내비 · 쿠폰 팝업
  components/
    Counselor.tsx         # 상담사 캐릭터(SVG) + 말풍선
    Charts.tsx            # 명식표 · 오행 · 대운 · 흐름 그래프 · 잠금 카드
  screens/
    Home.tsx  Detail.tsx  Result.tsx  Search.tsx  Library.tsx  Ask.tsx
```
